name: 測試用 - 使用 actions runner 

on:
  push:
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: 確認執行PATH
        run: |
          ls
          dir

      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
